#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <stdarg.h>
#include <string.h>
#include <unistd.h>

int _putchar(char c);
int print_number(int val, int num);
int _printf(const char * const format, ...);
int _puts(char *str);
#endif
