#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <stdarg.h>
#include <string.h>
#include <stdlib.h>
#include <unistd.h>
#include <limits.h>

int _putchar(char c);
int print_number(int val, int num);
int _strlen(char *str);
int _printf(const char * const format, ...);
int _puts(char *str);
#endif
